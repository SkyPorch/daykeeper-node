# Contract source

`daykeeper.yaml` was copied from `SkyPorch/daykeeper-openapi` tag `v1.0.0`,
commit `35f5bd45fe0c6a6901766543bff90dae6838b965`.

Generation pull requests must replace this file from an immutable
`daykeeper-openapi` tag and update this record with the tag and full commit SHA.
CI regenerates TypeScript declarations and fails when the committed output
differs.
